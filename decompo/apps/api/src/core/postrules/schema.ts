import { ActionStepSchema, GuideOutlineSchema, HelpResultSchema } from "@decompo/shared-schemas";

export const PostRuleSchemas = {
  action: ActionStepSchema,
  help: HelpResultSchema,
  guide: GuideOutlineSchema,
};
