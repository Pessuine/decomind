{
  "name": "decomind",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/shared-utils && npm run build -w packages/shared-schemas && npm run build -w apps/api && npm run build -w apps/web-h5 && npm run build -w apps/admin",
    "lint": "npm run lint -ws --if-present",
    "dev:api": "npm run dev -w apps/api",
    "dev:web": "npm run dev -w apps/web-h5",
    "dev:admin": "npm run dev -w apps/admin"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
